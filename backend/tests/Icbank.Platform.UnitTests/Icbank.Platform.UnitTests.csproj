<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>

    <!-- Why: the conventions doc mandates MethodUnderTest_Scenario_ExpectedOutcome test names,
         which trips CA1707 ("identifiers should not contain underscores"). Test-only, project-wide
         deviation — production code still enforces CA1707 via Directory.Build.props. -->
    <NoWarn>$(NoWarn);CA1707</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="FluentAssertions" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="Bogus" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Icbank.Platform.Domain\Icbank.Platform.Domain.csproj" />
    <ProjectReference Include="..\..\src\Icbank.Platform.Application\Icbank.Platform.Application.csproj" />
    <ProjectReference Include="..\..\src\Icbank.Platform.Infrastructure\Icbank.Platform.Infrastructure.csproj" />
    <!-- Why: StartupSecretsGuardExtensionsTests needs the real WebApplicationBuilder extension
         method from the Api project: re-implementing its logic in the test would only prove the
         copy matches itself, not that the deployed app is protected. -->
    <ProjectReference Include="..\..\src\Icbank.Platform.Api\Icbank.Platform.Api.csproj" />
  </ItemGroup>

</Project>
